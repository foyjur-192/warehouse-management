import React from 'react';

const NotFound = () => {
    return (
        <div>
           <h4 className='text-red'> This is page is not found back to Home</h4>
        </div>
    );
};

export default NotFound;